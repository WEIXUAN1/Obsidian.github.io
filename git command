工作区 ->(add)-> 暂存区->(commit)->本地仓库->(push)->远程仓库
﻿
常用git命令
git branch branchName    #创建branch
git checkout branchName  #切换branch
git merge subBrachName   #合并分支到master
git branch               #branch的list
git branch -d <name>     #删除分支
git push origin branch1  
git push origin --delete branchname

本地仓库修改后同步到远程仓库过程
git status               #查看新增、修改文件
cat filename             #查看文件内容
git add filename         #追加文件到版本库    
git commit -m "comment"  #提交文件到仓库  
git push origin master   #本地仓库分支同步到远程仓库

获取远程仓库分支
git pull origin master   #从远程获取最新版本并merge到本地


工作区修改撤销
git checkout -- filename  #filename在工作区的修改全部撤销
git reset HEAD filename   #该文件在暂存区的修改撤销                         --【git add filename】的反向操作
git reset --hard commitid #返回到某个特定版本
  查看该版本之前commitid记录
  git log
  查看所有版本变更记录的commitid
  git reflog





